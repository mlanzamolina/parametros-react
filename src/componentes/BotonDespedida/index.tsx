
import React from 'react';
import PropTypes from 'prop-types';
import { Button } from '@mui/material';

const BotonDespedida = () => {
    return (
        <Button variant="contained">Adios</Button>
      )
}

BotonDespedida.propTypes = {
    
}

export default BotonDespedida;
