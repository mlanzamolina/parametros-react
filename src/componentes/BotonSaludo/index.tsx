import React from 'react'
import PropTypes from 'prop-types'
import { Button } from '@mui/material'

const BotonSaludo = (props: any) => {
  return (
   <Button variant="contained">Hola</Button>
  )
}

BotonSaludo.propTypes = {}

export default BotonSaludo;